# 工作日志：设备点检数字系统部署

## 项目概述
将 `main.py`（PyQt5 OCR 桌面应用）改造为 Web 应用，部署到腾讯云轻量应用服务器。

---

## 项目结构

```
E:\claude\claude-projects-jpg\          ← 原始 Python 项目（PyQt5版）
├── main.py                             ← 桌面版主程序（未部署）
├── ocr_engine.py                       ← OCR 识别引擎（RapidOCR）
├── templates.py                        ← 设备模板定义
├── 1.jpg ~ 4.jpg                       ← 测试图片

E:\claude\web-app\                       ← Web 版（已部署）
├── index.html                          ← 前端页面（已修改为调用后端API）
├── .gitignore
├── 工作日志.md                          ← 本文件
├── backend/
│   ├── main.py                         ← FastAPI 后端（能提供前端页面）
│   ├── ocr_engine.py                   ← OCR 引擎（导入已改为 rapidocr_onnxruntime）
│   ├── templates.py
│   ├── requirements.txt
│   ├── Dockerfile
│   └── runtime.txt
```

---

## 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器 IP | **114.132.150.9** |
| 系统 | Windows Server 2022 DataCenter |
| Python 路径 | `C:\python\python.exe` |
| 代码路径 | `C:\Users\Administrator\device-ocr-system` |
| 启动端口 | **80** |
| 开机自启任务名 | `DeviceOCR-API` |
| GitHub 仓库 | `https://github.com/jjj-455/device-ocr-system` |
| Git 用户 | JJJ-455 |

---

## 访问方式

| 说明 | 地址 |
|------|------|
| 前端页面（浏览器打开） | `http://114.132.150.9` |
| API 健康检查 | `http://114.132.150.9/` |
| OCR 识别接口 | `POST http://114.132.150.9/api/ocr` |
| 设备列表接口 | `GET http://114.132.150.9/api/devices` |

---

## 功能说明

### 支持两种设备类型

**1. 励磁冷却系统（chiller）— 7 个参数**
- OCR 策略：`position_sort`（空间坐标排序）
- 测试图片：1.jpg, 2.jpg

**2. 循环水系统（circulating）— 19 个参数**
- OCR 策略：`label_match`（预设坐标匹配）
- 测试图片：3.jpg, 4.jpg

### 前端页面功能
- 上传图片 → 点「提取数据」→ OCR 自动识别
- 手动输入/修改参数
- 保存记录（IndexedDB 本地存储）
- 历史记录查询、编辑、删除
- 导入/导出 Excel

---

## 关键修复记录

| 问题 | 现象 | 原因 | 修复方式 |
|------|------|------|---------|
| 模块导入错误 | `ModuleNotFoundError: No module named 'rapidocr'` | 模块名是 `rapidocr_onnxruntime` | 第10行改为 `from rapidocr_onnxruntime import RapidOCR` |
| DLL 加载失败 | `DLL load failed while importing onnxruntime_pybind11_state` | 缺少 VC++ 运行库 | 安装 `vc_redist.x64.exe` |
| 新版返回格式 | `'tuple' object has no attribute 'txts'` | rapidocr 新版返回 (list, elapse) 元组 | `ocr_engine.py` 中兼容两种返回格式 |
| 函数名冲突 | `unexpected keyword argument 'strategy'` | 路由函数 `ocr_recognize` 与 import 的函数同名 | 路由函数改名为 `handle_ocr` |
| Tesseract 识别不准 | 数据错乱 | 浏览器端 Tesseract.js 精度远低于 PP-OCRv4 | 改为 Python FastAPI 后端调用 RapidOCR |

---

## 常用命令

### 本机（Windows 电脑 PowerShell）
```powershell
# 修改前端
cd E:\claude\web-app
notepad index.html

# 提交推送
git add .
git commit -m "描述修改内容"
git push
```

### 服务器（远程桌面 → cmd）
```cmd
# 拉取最新代码
cd C:\Users\Administrator\device-ocr-system
git pull

# 丢弃本地修改直接拉取
git checkout -- backend\main.py backend\ocr_engine.py
git pull

# 启动服务
cd backend
"C:\python\python.exe" -m uvicorn main:app --host 0.0.0.0 --port 80

# 验证服务
curl http://localhost:80

# 查看 Python 包列表
"C:\python\python.exe" -m pip list

# 清除缓存
rmdir /s /q __pycache__
```

---

## 后续待办

### [ ] 1. 更新开机自启脚本（端口改为 80）
当前 `start.bat` 可能是旧版（8000 端口），需要更新：

```cmd
echo "C:\python\python.exe" -m uvicorn main:app --host 0.0.0.0 --port 80 > C:\Users\Administrator\device-ocr-system\backend\start.bat
```

### [ ] 2. 重启服务器验证自启
```cmd
shutdown -r -t 0
```
重启后等 2 分钟，浏览器访问 `http://114.132.150.9` 看是否自动运行。

### [ ] 3. 全面测试所有图片

| 测试图片 | 设备类型 | 预期参数数 | 测试结果 |
|---------|---------|-----------|---------|
| 1.jpg | 励磁冷却系统 | 7 | □ |
| 2.jpg | 励磁冷却系统 | 7 | □ |
| 3.jpg | 循环水系统 | 19 | □ |
| 4.jpg | 循环水系统 | 19 | □ |

### [ ] 4. 测试手机访问
用手机浏览器打开 `http://114.132.150.9`，测试上传图片识别。

### [ ] 5. 优化循环水系统（如有需要）
`label_match` 策略中的坐标是写死的像素值，如果 3.jpg/4.jpg 识别不准，需要调整 `ocr_engine.py` 中的 `field_positions` 坐标数组。

### [ ] 6. 添加 HTTPS（长期）
目前是 HTTP 明文传输，如果需要更安全可以配置 SSL 证书。

---

## 部署记录

| 时间 | 事件 |
|------|------|
| 2026-06-04 | 项目初始化，分析 main.py 工作原理 |
| 2026-06-04 | 将 OCR 识图原理移植到 index.html（后废弃） |
| 2026-06-04 | 搭建 FastAPI 后端，创建 Render 部署配置 |
| 2026-06-04 | GitHub 仓库创建并推送代码 |
| 2026-06-05 | 腾讯云轻量服务器部署，安装 Python 3.12 |
| 2026-06-05 | 修复 rapidocr 导入问题、DLL 错误 |
| 2026-06-05 | 修复函数名冲突、新版返回格式兼容 |
| 2026-06-05 | 前后端合并部署到端口 80 |
| 2026-06-06 | 增加 SQLite 服务端存储，实现跨设备数据共享 |
| 2026-06-06 | 优化列表API，剥离base64图片数据，手机加载提速2000倍 |
